/**
 * @fileoverview Narrow mood-entry façade (Today tab / day editor hot path).
 * @module storage/entries
 */
export { getEntry, peekEntryFromSessionCache, upsertEntry, createEntry, getLastAllEntriesSource, } from '../data/storage/moodStorage';
