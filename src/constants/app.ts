/**
 * @fileoverview Release metadata (keep in sync with app.json `expo.version` and package.json).
 * @module constants/app
 */

/** User-facing release train (Expo `expo.version` / App Store version string). */
export const APP_RELEASE_VERSION = '2.0.0' as const;

/** Short product name for docs and UI. */
export const APP_DISPLAY_NAME = 'Moodly' as const;

/** Internal marketing / support label. */
export const APP_RELEASE_CODENAME = 'V2' as const;
