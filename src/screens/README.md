## Screens

Put full-screen UI here (layout + user intent).
Screens may call `src/storage` to load/save and `src/utils`/`src/logic` for pure rules.
Do not import AsyncStorage or deep internal modules.

Hot paths:
- `CalendarScreen.tsx` (month timeline)
- `CalendarView.tsx` (year pager)

