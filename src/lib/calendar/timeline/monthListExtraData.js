/**
 * @fileoverview extraData payload for FlashList month timeline (forces cell recycle refresh).
 * @module lib/calendar/timeline/monthListExtraData
 */
export function buildCalendarMonthListExtraData(parts) {
    return parts;
}
