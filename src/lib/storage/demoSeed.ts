/**
 * @fileoverview Demo seed (legacy import surface).
 * @module lib/storage/demoSeed
 *
 * Phase 2: implementation moved to `src/data/storage/*`.
 * Keep this file as a re-export so older imports keep working.
 */

export * from '../../data/storage/demoSeed';

