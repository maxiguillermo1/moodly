/**
 * @fileoverview AsyncStorage wrapper for mood entries (legacy import surface).
 * @module lib/storage/moodStorage
 *
 * Phase 2: implementation moved to `src/data/storage/*`.
 * Keep this file as a re-export so older imports keep working.
 */

export * from '../../data/storage/moodStorage';
