/**
 * @fileoverview Logic layer public surface (pure rules + canonical model).
 * @module logic
 *
 * Beginner rule:
 * - No React, no navigation, no storage.
 * - Deterministic transforms and validation helpers live here.
 */

export * from '../data/model';

