/**
 * @fileoverview App settings types
 * @module types/settings
 */
/** Default top-to-bottom order for Today extension slots (overridden when users re-enable toggles). */
export const DEFAULT_TODAY_EXTENSIONS_STACK_ORDER = [
    'habits',
    'goals',
    'todo',
];
