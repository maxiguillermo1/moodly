/**
 * @fileoverview Cloud auth hook (re-export for UI layer).
 * @module hooks/useAuth
 */

export { useAuth } from '../cloud/auth/AuthContext';
export type { Session, User } from '../cloud/auth/AuthContext';
