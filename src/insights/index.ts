/**
 * @fileoverview Insights layer public surface (pure derived selectors/aggregations).
 * @module insights
 *
 * Beginner rule:
 * - Pure functions only.
 * - Used for analytics-ready derived views (daily/weekly/monthly/streaks).
 */

export * from '../data/analytics';

