## Domain

Pure rules and derived insights live here (no React, no storage).
Use this for date keys, validation helpers, and selectors/aggregations.
If it needs AsyncStorage or UI, it does not belong here.

